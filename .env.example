# Copy to .env to override defaults. Nothing here is required: the stack comes up
# with `docker compose up --build` as-is, no keys and no edits.

# Grafana admin login. Default is admin / admin (Grafana prompts for a new
# password on first sign-in).
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin
